#include <string>
#include <bitset>

class Solution {
public:
    std::string addBinary(std::string a, std::string b) {
        unsigned int aSize = a.size();
        const unsigned int bSize = b.size();

        std::bitset<aSize> d;

        return "";
    }
};