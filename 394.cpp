#include <string>
#include <stack>
#include <algorithm> // reverse

class Solution {
public:
    std::string decodeString(std::string s) {
        std::string r;
        std::stack<char> cs;
        std::stack<int> is;

        while (s.size() != 0) {
            
        }

        return r;
    }
};

int main() {
    Solution s;

    std::string s1 = "3[a]2[bc]";

    s.decodeString(s1);

    return 0;
}